services:
  backend:
    build:
      context: ..
      dockerfile: infra/backend.Dockerfile
    environment:
      PORT: "4000"
      DATA_DIR: /app/apps/backend/data
      ADMIN_PANEL_PASSWORD: ${ADMIN_PANEL_PASSWORD}
    volumes:
      - backend-data:/app/apps/backend/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:4000/healthz').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 10s

  frontend:
    build:
      context: ..
      dockerfile: infra/frontend.Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-https://cashier.ko-lab.space/rpc}
        VITE_IBAN: ${VITE_IBAN}
        VITE_IBAN_NAME: ${VITE_IBAN_NAME}
    ports:
      - "4173:4173"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:4173/"]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 10s

volumes:
  backend-data:
